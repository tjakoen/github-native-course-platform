// Static registry of the teacher-repo workflows the console can run or watch.
// Input shapes verified against the live repos 2026-07-25 (the engine keeps
// these identical across sections). `gate` names the input that flips a run
// from dry-run to a real write - the UI locks it behind a typed confirm.
// `danger` is the confirm phrasing. Workflows with dispatch:false are
// status-only rows (they trigger on push/schedule).
export const OPS = [
  { file: "grade.yml", note: "Idempotent: sha-keyed, re-runs only grade new or changed submissions.", label: "Grade sweep", group: "Grading",
    desc: "Clone + grade submissions against the canonical tests; writes the gradebook (teacher-side only).",
    inputs: [
      { name: "dry_run", type: "bool", def: "true", gate: true, invert: true, danger: "write the gradebook" },
      { name: "only", type: "string", def: "", activity: true, hint: "one activity id (e.g. m1a2)" },
      { name: "repo", type: "string", def: "", hint: "one exact repo name" },
      { name: "force", type: "bool", def: "false" },
    ] },
  { file: "publish.yml", note: "Idempotent: only activities flagged publish:true, unchanged files skipped.", label: "Publish grades", group: "Delivery",
    desc: "THE ONLY writer of student repos: delivers GRADES.md + FEEDBACK.md for publish:true activities.",
    inputs: [
      { name: "publish", type: "bool", def: "false", gate: true, danger: "push grades into student repos" },
      { name: "only", type: "string", def: "", activity: true, hint: "one activity id" },
      { name: "repo", type: "string", def: "", hint: "one source repo" },
    ] },
  { file: "canvas-push.yml", note: "Execute is an idempotent upsert; check mode only reads the roster.", label: "Canvas push", group: "Canvas",
    desc: "Push gradebook scores into Canvas (check -> dry-run -> execute).",
    inputs: [
      { name: "mode", type: "choice", options: ["check", "dry-run", "execute"], def: "dry-run", gate: "execute", danger: "write grades to Canvas" },
      { name: "course_id", type: "string", def: "", hint: "override CANVAS_COURSE_ID" },
      { name: "comment", type: "bool", def: "false" },
    ] },
  { file: "canvas-sync-assignments.yml", note: "Conservative on existing assignments: points only, unless desc/submit/rename are set. Never touches due dates or published state.", label: "Canvas assignment sync", group: "Canvas",
    desc: "Author/update the Canvas assignment shells from grader/assignments.json (SUBMISSIONS module).",
    inputs: [
      { name: "mode", type: "choice", options: ["dry-run", "execute"], def: "dry-run", gate: "execute", danger: "write assignments to Canvas" },
      { name: "only", type: "string", def: "", activity: true, hint: "one activity id" },
      { name: "desc", type: "bool", def: "false" }, { name: "submit", type: "bool", def: "false" }, { name: "rename", type: "bool", def: "false" },
    ] },
  { file: "canvas-export.yml", label: "Canvas grade export", group: "Canvas",
    desc: "Build a Canvas-import CSV from the gradebook (paste the Canvas export in).",
    inputs: [ { name: "canvas_csv", type: "text", def: "" }, { name: "section", type: "string", def: "" } ] },
  { file: "canvas-crosscheck.yml", note: "Read-only against Canvas; commits reports/canvas-crosscheck.md (open it under Reports).", label: "Canvas cross-check", group: "Canvas",
    desc: "Audit the gradebook vs Canvas: identity match, submission presence, screenshot readability, and score agreement." },
  { file: "publish-material.yml", note: "Idempotent per unit. Units run ONE AT A TIME (parallel unit runs used to race) - the console dispatches them sequentially.", label: "Publish material", group: "Content",
    desc: "Push one content/ unit into every student workspace (direct course-bot commits). Run units one at a time.",
    inputs: [ { name: "unit", type: "string", def: "", required: true, gateAlways: true, danger: "push this unit to every workspace" } ] },
  { file: "provision-workspaces.yml", note: "Creates what is missing; never deletes or renames.", label: "Provision workspaces", group: "Hygiene",
    desc: "Create missing student workspaces + backfill blank student.json from submissions.",
    inputs: [ { name: "execute", type: "bool", def: "false", gate: true, danger: "create/modify workspace repos" }, { name: "only", type: "string", def: "", hint: "one student handle" } ] },
  { file: "prune-gradebook.yml", note: "Removes a row only on a definitive repo 404.", label: "Prune gradebook", group: "Hygiene",
    desc: "Drop gradebook rows whose submission repo 404s (deleted/renamed).",
    inputs: [ { name: "execute", type: "bool", def: "false", gate: true, danger: "rewrite grades.csv" } ] },
  { file: "resolve-identities.yml", note: "Reads the live Canvas roster; fixes only unambiguous typo/blank rows, reports the rest.", label: "Resolve identities", group: "Hygiene",
    desc: "Fix blank/typo'd studentNumber rows by copying a Canvas-verified sibling's student.json into the bad submission repo.",
    inputs: [ { name: "execute", type: "bool", def: "false", gate: true, danger: "write student.json into student submission repos" } ] },
  { file: "prune-phantom-activities.yml", note: "Removes an entry only if it has NO Canvas assignment AND no gradebook rows.", label: "Prune phantom activities", group: "Hygiene",
    desc: "Drop phantom activities (no Canvas assignment and no grades) from grader/assignments.json.",
    inputs: [ { name: "execute", type: "bool", def: "false", gate: true, danger: "rewrite grader/assignments.json" } ] },
  { file: "audit-names.yml", label: "Audit repo names", group: "Hygiene",
    desc: "Weekly naming/identity audit. A RED run means it FOUND problems (by design).",
    dispatch: false },
  { file: "repo-coverage.yml", note: "Read-only; commits reports/repo-coverage.md (open it under Reports).", label: "Repo coverage audit", group: "Hygiene",
    desc: "Every activity submission repo on the org vs the gradebook: flags ungraded repos and activities missing from assignments.json." },
  { file: "generate-attendance-qrs.yml", note: "Skips workspaces that already have a QR unless force.", label: "Generate attendance QRs", group: "Attendance",
    desc: "Sign + commit each student's QR into their workspace; refresh roster.json.",
    inputs: [ { name: "execute", type: "bool", def: "false", gate: true, danger: "write QRs into workspaces" }, { name: "force", type: "bool", def: "false" }, { name: "only", type: "string", def: "", hint: "one student handle" } ] },
  { file: "verify-attendance.yml", label: "Verify attendance", group: "Attendance",
    desc: "Runs on every scan-batch push: verifies signatures, rebuilds summaries. RED = flagged scan.",
    dispatch: false },
  { file: "publish-attendance.yml", note: "Idempotent: an unchanged record writes nothing.", label: "Publish attendance receipts", group: "Attendance",
    desc: "Deliver each student their own MY-ATTENDANCE.md (auto after verify; manual re-run here).",
    inputs: [ { name: "execute", type: "bool", def: "false", gate: true, danger: "write receipts into workspaces" }, { name: "only", type: "string", def: "", hint: "one student handle" } ] },
];
