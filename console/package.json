{
  "name": "course-console",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Course Console: the hosted management + review surface for the GitHub-native course platform: a data-free GitHub Pages shell that reads teacher-repo gradebooks live from the GitHub API in the browser and generates the prompts the AI runs to apply decisions. Uses the GRAIN design system.",
  "dependencies": {
    "@tjakoen/grain": "^0.1.12"
  },
  "scripts": {
    "bake": "node scripts/bake-theme.mjs",
    "check:intents": "node scripts/check-intents.mjs",
    "dev": "node scripts/bake-theme.mjs && npx serve site",
    "audit": "node src/audit-students.mjs",
    "fix": "node src/apply-fixes.mjs",
    "blanks": "node src/check-blanks.mjs",
    "test:e2e": "node scripts/e2e-shots.mjs",
    "test:demo": "node scripts/e2e-demo.mjs"
  },
  "devDependencies": {
    "@tjakoen/crumb": "^0.1.4",
    "@tjakoen/mill": "^0.1.2",
    "esbuild": "^0.28.1",
    "playwright": "^1.62.0"
  }
}
